# Copy to .env and paste your key. Never commit .env.
OPENAI_API_KEY=

# Everything else is optional: config.py declares every setting with a default.
# Override one here only when you want to change it, e.g.
#   TOP_K=8
